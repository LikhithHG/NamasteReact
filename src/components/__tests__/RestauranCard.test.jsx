import { screen, render, fireEvent } from "@testing-library/react";
import RestauranCard from "../RestauranCard";

it("render restaurant card component with props data", () => {
    
    //Create Mock data just testing -  just hard code for testing code
    //import this mock data

    /**
        render(<RestauranCard resData ={MOCKDATA} />);
        const name = screen.getByText("Name of the restaurant");
        expect(name).toBeInTheDocumet();
    */
});


//Test case to render with promoted label - higher order component